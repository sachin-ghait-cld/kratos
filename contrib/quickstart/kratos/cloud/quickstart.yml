version: '3.7'

services:
  kratos:
    volumes:
      - type: bind
        source: ./contrib/quickstart/kratos/cloud
        target: /etc/config/kratos
  kratos-migrate:
    volumes:
      - type: bind
        source: ./contrib/quickstart/kratos/cloud
        target: /etc/config/kratos
